machine:
    timezone:
        America/Los_Angeles

dependencies:
    cache_directories:
      - "frontend/node_modules"
      - "frontend/bower_components"
      - "../pyandoc"
    override:
      - fab install_deps

deployment:
    production:
        branch: master
        commands:
            - fab build_all && fab deploy
test:
  override:
    - fab build_all && fab test
