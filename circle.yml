machine:
    timezone:
        America/Los_Angeles

deployment:
    production:
        branch: master
        commands:
            - fab build_all && fab deploy
test:
  override:
    - fab install_frontend_deps && fab test
