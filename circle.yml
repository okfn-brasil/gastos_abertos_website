machine:
    timezone:
        America/Los_Angeles

dependencies:
    cache_directories:
      - "frontend/node_modules"
      - "frontend/bower_components"
    override:
      - fab install_deps

deployment:
    production:
        branch: master
        commands:
            - fab build_all && fab deploy
test:
  override:
    - fab test
